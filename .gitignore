build/
*.o
*.obj
.env
storage/uploads/*
!storage/uploads/.gitkeep
data/uploads/*
!data/uploads/.gitkeep

# Drogon's own multipart-parser scratch directory (created at the process's
# working directory when the server runs, e.g. `backend/uploads/tmp/`) —
# unrelated to our FILE_STORAGE_PATH, safe to ignore entirely.
uploads/
